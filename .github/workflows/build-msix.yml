name: Build MSIX Bundle

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      configuration:
        description: "Build configuration (Debug/Release)"
        required: false
        default: "Release"

jobs:
  build-msix:
    runs-on: windows-latest
    env:
      CONFIGURATION: ${{ github.event.inputs.configuration || 'Release' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup NuGet
        uses: NuGet/setup-nuget@v2

      - name: Restore NuGet packages
        run: nuget restore Launcher.sln

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v2

      - name: Build MSIX bundle
        shell: pwsh
        run: |
          $packageDir = Join-Path $env:RUNNER_TEMP "msix"
          New-Item -ItemType Directory -Force -Path "$packageDir" | Out-Null
          msbuild "MSIX Installer/MSIX Installer.wapproj" `
            /p:Configuration=$env:CONFIGURATION `
            /p:AppxBundle=Always `
            /p:AppxBundlePlatforms="x86|x64|ARM64" `
            /p:UapAppxPackageBuildMode=StoreUpload `
            /p:GenerateAppInstallerFile=false `
            /p:AppxPackageDir="$packageDir" `
            /p:AppxPackageSigningEnabled=false
          echo "PACKAGE_DIR=$packageDir" >> $env:GITHUB_ENV

      - name: Locate bundle
        id: find_bundle
        shell: pwsh
        run: |
          $packageDir = $env:PACKAGE_DIR
          if (-not $packageDir) { $packageDir = Join-Path $env:RUNNER_TEMP "msix" }
          $bundle = Get-ChildItem -Path "$packageDir" -Filter *.msixbundle -Recurse | Select-Object -First 1
          if (-not $bundle) {
            Write-Error "Keine .msixbundle gefunden."
            exit 1
          }
          echo "bundle_path=$($bundle.FullName)" >> $env:GITHUB_OUTPUT
          echo "Bundle: $($bundle.FullName)"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: msixbundle
          path: ${{ steps.find_bundle.outputs.bundle_path }}

      - name: Release asset
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        with:
          files: ${{ steps.find_bundle.outputs.bundle_path }}
